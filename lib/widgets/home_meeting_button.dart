import 'package:flutter/material.dart';

class HomeMeetingButton extends StatelessWidget {
  const HomeMeetingButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
